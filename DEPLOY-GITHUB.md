# نشر منجز على GitHub

## الخيار الأسرع: GitHub Pages

هذا الخيار مناسب لعرض الموقع والتصميم كرابط عام.

1. افتحي GitHub وسوي مستودع جديد باسم `munjaz`.
2. ارفعي ملفات المشروع كاملة ما عدا الملفات الموجودة في `.gitignore`.
3. ادخلي على المستودع:
   - Settings
   - Pages
   - Source: Deploy from a branch
   - Branch: `main`
   - Folder: `/root`
4. بعد دقائق يعطيك GitHub رابط مثل:

```text
https://USERNAME.github.io/munjaz/
```

## ربط الدومين

بعد تشغيل GitHub Pages:

1. من Settings ثم Pages اكتبي الدومين في خانة Custom domain.
2. من لوحة الدومين أضيفي سجلات GitHub Pages حسب نوع الدومين:
   - للدومين الرئيسي استخدمي سجلات `A`.
   - للـ `www` استخدمي سجل `CNAME`.
3. فعلي Enforce HTTPS بعد ما يجهز الرابط.

## ملاحظة مهمة

GitHub Pages يشغل الواجهة فقط. يعني تسجيل الدخول والحفظ يشتغلان كتجربة محلية داخل المتصفح.

لما تبين نسخة رسمية بحسابات وبيانات مشتركة لكل المعلمين، ننشر السيرفر `server.js` على استضافة تدعم Node.js ونربطه بقاعدة بيانات.
